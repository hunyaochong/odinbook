<nav>
  <ul>
    <li class="nav-left">Odinbook - X clone</li>
    <div class="nav-right">
        <% unless user_signed_in? %>
            <li><%= link_to 'Sign in', new_user_session_path %></li>
            <li><%= link_to 'Register', new_user_registration_path %></li>
        <% else %>
            <li><%= link_to 'Sign out', destroy_user_session_path %></li>
        <% end %>
    </div>
  </ul>
</nav>